{
    "version": 2,
    "buildCommand": "npm run build",
    "outputDirectory": "out",
    "installCommand": "npm install",
    "functions": {
        "app/**/*.ts": {
            "includeFiles": "**/*"
        }
    },
    "env": {
        "NODE_ENV": "production"
    }
}